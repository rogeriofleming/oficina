diff --git a/build/gulpfile.vscode.win32.ts b/build/gulpfile.vscode.win32.ts
index c6f551d..c7d9205 100644
--- a/build/gulpfile.vscode.win32.ts
+++ b/build/gulpfile.vscode.win32.ts
@@ -88,6 +88,9 @@ function buildWin32Setup(arch: string, target: string): task.CallbackTask {
 		const definitions: Record<string, unknown> = {
 			NameLong: product.nameLong,
 			NameShort: product.nameShort,
+			// The folder under %APPDATA% the built product will use. Same fallback as the runtime, so the
+			// installer always writes to the folder the program will read from.
+			UserDataFolderName: (product as typeof product & { userDataFolderName?: string }).userDataFolderName || product.nameShort,
 			DirName: product.win32DirName,
 			Version: pkg.version,
 			RawVersion: pkg.version.replace(/-\w+$/, ''),
diff --git a/src/main.ts b/src/main.ts
index 085290b..f62225a 100644
--- a/src/main.ts
+++ b/src/main.ts
@@ -54,7 +54,10 @@ if (args['sandbox'] &&
 }
 
 // Set userData path before app 'ready' event
-const userDataPath = getUserDataPath(args, product.nameShort ?? 'code-oss-dev');
+// A product may keep the user data of two editions of itself apart with `userDataFolderName`,
+// without renaming the product (and therefore the executable) for one of them. Absent, the folder
+// is `nameShort`, exactly as before. See patches/0015.
+const userDataPath = getUserDataPath(args, product.userDataFolderName ?? product.nameShort ?? 'code-oss-dev');
 if (process.platform === 'win32') {
 	const userDataUNCHost = getUNCHost(userDataPath);
 	if (userDataUNCHost) {
diff --git a/src/vs/platform/environment/node/environmentService.ts b/src/vs/platform/environment/node/environmentService.ts
index 8652144..a3a25c1 100644
--- a/src/vs/platform/environment/node/environmentService.ts
+++ b/src/vs/platform/environment/node/environmentService.ts
@@ -17,7 +17,9 @@ export class NativeEnvironmentService extends AbstractNativeEnvironmentService {
 		super(args, {
 			homeDir,
 			tmpDir: tmpdir(),
-			userDataDir: getUserDataPath(args, productService.nameShort),
+			// Same key the main entry point reads, for the same reason: two editions of one product may
+			// need separate user data without a different product name. Absent, it is `nameShort`.
+			userDataDir: getUserDataPath(args, productService.userDataFolderName ?? productService.nameShort),
 		}, productService);
 	}
 }
