diff --git a/src/vs/platform/extensionManagement/node/extensionManagementService.ts b/src/vs/platform/extensionManagement/node/extensionManagementService.ts
index 6eefd7163db..f526111792b 100644
--- a/src/vs/platform/extensionManagement/node/extensionManagementService.ts
+++ b/src/vs/platform/extensionManagement/node/extensionManagementService.ts
@@ -34,7 +34,6 @@ import {
 	ExtensionSignatureVerificationCode,
 	computeSize,
 	IAllowedExtensionsService,
-	VerifyExtensionSignatureConfigKey,
 	shouldRequireRepositorySignatureFor,
 } from '../common/extensionManagement.js';
 import { areSameExtensions, computeTargetPlatform, ExtensionKey, getGalleryExtensionId, groupByExtension } from '../common/extensionManagementUtil.js';
@@ -87,7 +86,12 @@ export class ExtensionManagementService extends AbstractExtensionManagementServi
 		@IDownloadService private downloadService: IDownloadService,
 		@IInstantiationService private readonly instantiationService: IInstantiationService,
 		@IFileService private readonly fileService: IFileService,
-		@IConfigurationService private readonly configurationService: IConfigurationService,
+		// Recebido, mas nao guardado: o patch 0003 removeu a unica leitura que esta
+		// classe fazia daqui (a chave de verificacao de assinatura de extensao). Manter
+		// `private readonly` deixaria uma propriedade que ninguem le — TS6138, que o
+		// empacotamento tolera e a compilacao de desenvolvimento NAO. O parametro
+		// continua na assinatura para nao mexer na ordem de injecao dos servicos.
+		@IConfigurationService configurationService: IConfigurationService,
 		@IExtensionGalleryManifestService protected readonly extensionGalleryManifestService: IExtensionGalleryManifestService,
 		@IProductService productService: IProductService,
 		@IAllowedExtensionsService allowedExtensionsService: IAllowedExtensionsService,
@@ -339,8 +343,14 @@ export class ExtensionManagementService extends AbstractExtensionManagementServi
 
 	private async downloadExtension(extension: IGalleryExtension, operation: InstallOperation, verifySignature: boolean, clientTargetPlatform?: TargetPlatform): Promise<{ readonly location: URI; readonly verificationStatus: ExtensionSignatureVerificationCode | undefined }> {
 		if (verifySignature) {
-			const value = this.configurationService.getValue(VerifyExtensionSignatureConfigKey);
-			verifySignature = isBoolean(value) ? value : true;
+			// OFICINA (patch de viabilidade): a verificacao de assinatura depende do modulo
+			// @vscode/vsce-sign, que e proprietario e NAO existe numa build aberta. Sem ele,
+			// verify() devolve undefined ('nao executou') e TODA instalacao vinda da loja e
+			// recusada com 'Signature verification was not executed' - inclusive extensao que
+			// a Open VSX assinou. Medido em 05/09/2026 nesta build.
+			// Mesma solucao do VSCodium (patches/00-extension-disable-signature-verification).
+			// O custo esta declarado no .md ao lado: a OFICINA nao verifica assinatura.
+			verifySignature = false;
 		}
 		const { location, verificationStatus } = await this.extensionsDownloader.download(extension, operation, verifySignature, clientTargetPlatform);
 		const shouldRequireSignature = shouldRequireRepositorySignatureFor(extension.private, await this.extensionGalleryManifestService.getExtensionGalleryManifest());
